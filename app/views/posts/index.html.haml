.post
  .add_new_post
    = link_to "add new post", new_post_path, class:"deco_none"
  = month_calendar events: @posts do |date, posts|
    .date
      = date.day
    - posts.each do |post|
      = link_to converting_to_jpy_for_calender(post.p_price), post_path(post.id), class: "deco_none_red price font-small"
      
  -#   -# - revenues.each do |revenue|
  -#   -#   = link_to converting_to_jpy_for_calender(revenue.r_price), revenue_path(revenue.id), class: "deco_none price font-small"
  -#   -#   %br

  -# = month_calendar events: @arry do |date, arry|
  -#   .date
  -#     = date.day
  -#   - arry.each do |arry|
  -#     arry.p_price
